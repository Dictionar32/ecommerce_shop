import { tw } from "tailwind-styled-v4"

export const StarContainer = tw.div({ base: "flex items-center gap-0.5" })
export const StarFilled = tw.div({ base: "text-gold-400 fill-gold-400" })
export const StarEmpty = tw.div({ base: "text-obsidian-700" })
