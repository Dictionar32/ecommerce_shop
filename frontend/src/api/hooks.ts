// Auto-generated by routesync. Do not edit manually.

import { useApiQuery, useApiMutation } from 'routesync/react'
import { api } from './api'

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.register.post, options)` for future compatibility.
 */
export function useRegisterPost() {
  return useApiMutation(api.register.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.login.post, options)` for future compatibility.
 */
export function useLoginPost() {
  return useApiMutation(api.login.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.oauth.getProviderRedirect, ...args)` for future compatibility.
 */
export function useOauthGetProviderRedirect(...args: Parameters<typeof api.oauth.getProviderRedirect>) {
  return useApiQuery(api.oauth.getProviderRedirect, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.oauth.getProviderCallback, ...args)` for future compatibility.
 */
export function useOauthGetProviderCallback(...args: Parameters<typeof api.oauth.getProviderCallback>) {
  return useApiQuery(api.oauth.getProviderCallback, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.oauth.postProviderCallback, options)` for future compatibility.
 */
export function useOauthPostProviderCallback() {
  return useApiMutation(api.oauth.postProviderCallback)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.social.postLogin, options)` for future compatibility.
 */
export function useSocialPostLogin() {
  return useApiMutation(api.social.postLogin)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.forgotPassword.post, options)` for future compatibility.
 */
export function useForgotPasswordPost() {
  return useApiMutation(api.forgotPassword.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.resetPassword.post, options)` for future compatibility.
 */
export function useResetPasswordPost() {
  return useApiMutation(api.resetPassword.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.categories.get, ...args)` for future compatibility.
 */
export function useCategoriesGet(...args: Parameters<typeof api.categories.get>) {
  return useApiQuery(api.categories.get, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.produk.get, ...args)` for future compatibility.
 */
export function useProdukGet(...args: Parameters<typeof api.produk.get>) {
  return useApiQuery(api.produk.get, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.produk.getId, ...args)` for future compatibility.
 */
export function useProdukGetId(...args: Parameters<typeof api.produk.getId>) {
  return useApiQuery(api.produk.getId, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.produk.getIdReviews, ...args)` for future compatibility.
 */
export function useProdukGetIdReviews(...args: Parameters<typeof api.produk.getIdReviews>) {
  return useApiQuery(api.produk.getIdReviews, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.produk.postIdReviews, options)` for future compatibility.
 */
export function useProdukPostIdReviews() {
  return useApiMutation(api.produk.postIdReviews)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.payment.postWebhook, options)` for future compatibility.
 */
export function usePaymentPostWebhook() {
  return useApiMutation(api.payment.postWebhook)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.payment.postOrderId, options)` for future compatibility.
 */
export function usePaymentPostOrderId() {
  return useApiMutation(api.payment.postOrderId)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.profile.get, ...args)` for future compatibility.
 */
export function useProfileGet(...args: Parameters<typeof api.profile.get>) {
  return useApiQuery(api.profile.get, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.profile.put, options)` for future compatibility.
 */
export function useProfilePut() {
  return useApiMutation(api.profile.put)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.profile.patch, options)` for future compatibility.
 */
export function useProfilePatch() {
  return useApiMutation(api.profile.patch)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.orders.get, ...args)` for future compatibility.
 */
export function useOrdersGet(...args: Parameters<typeof api.orders.get>) {
  return useApiQuery(api.orders.get, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.orders.getId, ...args)` for future compatibility.
 */
export function useOrdersGetId(...args: Parameters<typeof api.orders.getId>) {
  return useApiQuery(api.orders.getId, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.orders.getIdInvoice, ...args)` for future compatibility.
 */
export function useOrdersGetIdInvoice(...args: Parameters<typeof api.orders.getIdInvoice>) {
  return useApiQuery(api.orders.getIdInvoice, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.cart.postItems, options)` for future compatibility.
 */
export function useCartPostItems() {
  return useApiMutation(api.cart.postItems)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.cart.patchItemsProdukItemId, options)` for future compatibility.
 */
export function useCartPatchItemsProdukItemId() {
  return useApiMutation(api.cart.patchItemsProdukItemId)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.cart.deleteItemsProdukItemId, options)` for future compatibility.
 */
export function useCartDeleteItemsProdukItemId() {
  return useApiMutation(api.cart.deleteItemsProdukItemId)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.cart.delete, options)` for future compatibility.
 */
export function useCartDelete() {
  return useApiMutation(api.cart.delete)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.cart.postPromo, options)` for future compatibility.
 */
export function useCartPostPromo() {
  return useApiMutation(api.cart.postPromo)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.cart.deletePromo, options)` for future compatibility.
 */
export function useCartDeletePromo() {
  return useApiMutation(api.cart.deletePromo)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.checkout.post, options)` for future compatibility.
 */
export function useCheckoutPost() {
  return useApiMutation(api.checkout.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.buyNow.post, options)` for future compatibility.
 */
export function useBuyNowPost() {
  return useApiMutation(api.buyNow.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.keranjang.get, ...args)` for future compatibility.
 */
export function useKeranjangGet(...args: Parameters<typeof api.keranjang.get>) {
  return useApiQuery(api.keranjang.get, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiQuery(api.wishlist.get, ...args)` for future compatibility.
 */
export function useWishlistGet(...args: Parameters<typeof api.wishlist.get>) {
  return useApiQuery(api.wishlist.get, ...args as any)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.wishlist.post, options)` for future compatibility.
 */
export function useWishlistPost() {
  return useApiMutation(api.wishlist.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.wishlist.deleteProdukItemId, options)` for future compatibility.
 */
export function useWishlistDeleteProdukItemId() {
  return useApiMutation(api.wishlist.deleteProdukItemId)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.logout.post, options)` for future compatibility.
 */
export function useLogoutPost() {
  return useApiMutation(api.logout.post)
}

/**
 * @deprecated Generated hooks will become optional in v2.
 * Prefer `useApiMutation(api.admin.postProduk, options)` for future compatibility.
 */
export function useAdminPostProduk() {
  return useApiMutation(api.admin.postProduk)
}
