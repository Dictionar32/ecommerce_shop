import { tw } from "tailwind-styled-v4"

export const StyledBody = tw.body({ base: "antialiased bg-obsidian-950 text-obsidian-50" })
export const AppContainer = tw.div({ base: "flex flex-col min-h-screen" })
export const MainContent = tw.main({ base: "grow" })
